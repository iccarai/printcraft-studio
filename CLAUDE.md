# PrintCraft Studio — Claude Instructions

## Deployment Verification

After every push to `main`, I must proactively monitor and verify the deployment:

- The user will **never** check Vercel logs, deployments, or test the live site themselves
- It is **my job** to verify changes are working correctly after every push
- Check Vercel deployment status, review logs for errors, and confirm the expected behaviour is live before considering any task done
